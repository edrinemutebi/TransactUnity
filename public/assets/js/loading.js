window.onload = function() {
    // All scripts and resources have loaded at this point
    const mainContent = document.getElementById('mainContent');  
    if (mainContent) {
      mainContent.classList.remove('blur-effect'); // Remove the blur effect
    }
};
  